{
    "buildCommand": "expo export --platform web --output-dir dist/app && cp -r landing/* dist/",
    "outputDirectory": "dist",
    "installCommand": "npm install",
    "framework": null,
    "rewrites": [
        {
            "source": "/app/:path*",
            "destination": "/app/index.html"
        }
    ]
}